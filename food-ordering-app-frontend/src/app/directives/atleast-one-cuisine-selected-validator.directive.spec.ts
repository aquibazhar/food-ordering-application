import { AtleastOneCuisineSelectedValidatorDirective } from './atleast-one-cuisine-selected-validator.directive';

describe('AtleastOneCuisineSelectedValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AtleastOneCuisineSelectedValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
