import { FilterFoodItemsPipe } from './filter-food-items.pipe';

describe('FilterFoodItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFoodItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
