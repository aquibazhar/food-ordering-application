import { DistanceEstimationPipe } from './distance-estimation.pipe';

describe('DistanceEstimationPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceEstimationPipe();
    expect(pipe).toBeTruthy();
  });
});
