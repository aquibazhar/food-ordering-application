import { CuisineSeparatorPipe } from './cuisine-separator.pipe';

describe('CuisineSeparatorPipe', () => {
  it('create an instance', () => {
    const pipe = new CuisineSeparatorPipe();
    expect(pipe).toBeTruthy();
  });
});
